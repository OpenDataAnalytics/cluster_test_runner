- !binder
  vars:
    global_var: "global_var"
  playbooks:
    - !playbook
      path: "ansible/test.yml"
      vars:
        test_var: "test_var"

    - !playbook
      path: "ansible/test_run.yml"
      vars:
        test_run_var: "test_run_var"
